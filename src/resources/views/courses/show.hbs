<div class="mt-4">
    <div class="row align-items-center shadow p-4 rounded">
        <div class="col-5">
            <img src="{{course.image}}" class="" alt="..." style="width: 500px;">
        </div>
        <div class="col-7">
            <div>
                <h5>{{course.name}}</h5>
                <p>{{course.description}}</p>
                <p><small class="text-body-secondary">{{course.createdAt}}</small></p>
            </div>
        </div>
    </div>
</div>
